import React, { Component } from 'react';

class EventListButton extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <button>Event List Button</button>
    );
  }
}

export default EventListButton;
